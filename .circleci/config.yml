version: 2
jobs:
  test:
    machine:
      image: circleci/classic:201808-01
    steps:
      - checkout:
      - run: make NETWORK=test dockertest
workflows:
  version: 2
  test:
    jobs:
      - test