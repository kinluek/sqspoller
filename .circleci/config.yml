version: 2
jobs:
  test:
    docker:
      - image: circleci/golang:latest
    working_directory: /go/src/github.com/mirrorweb/dora-go
    steps:
      - checkout:
      - run: docker version
      - run: go test -v ./...
workflows:
  version: 2
  test:
    jobs:
      - test